// Returns true if the right num is less than left num.
// Thus, sort the array of numbers in ascending order.

function compareNums(left, right) {
  return right < left;
}

export default compareNums;